export type PlayerProps = {
    playerName: string;
    playerImage: string;
    totalEvents: number;
    sports: string;
};

export type CardProps = {
    adImage: string;
    adTitle: string;
    adDesc: string;
};

export type SpotlightCardProps = {
    title: string;
    image: string;
    date: string;
    day: string;
    time: string;
    address: string;
    collection: string;
};
